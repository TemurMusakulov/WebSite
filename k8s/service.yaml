apiVersion: v1
kind: Service
metadata:
  name: temur-portfolio
  labels:
    app: temur-portfolio
spec:
  type: ClusterIP
  selector:
    app: temur-portfolio
  ports:
    - name: http
      port: 80
      targetPort: 80
